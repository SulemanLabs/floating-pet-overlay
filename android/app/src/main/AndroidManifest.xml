<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Lets OverlayWindowManager draw the pet above other apps via
         TYPE_APPLICATION_OVERLAY. This is a "special" permission: it is not
         granted through the normal runtime dialog, but through the
         Settings.ACTION_MANAGE_OVERLAY_PERMISSION screen — see
         MainActivity.requestOverlayPermission(). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Required on API 28+ to run OverlayService as a foreground service at all. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- OverlayService declares foregroundServiceType="specialUse" because
         drawing a floating overlay doesn't match any of the more specific
         typed categories (location, camera, mediaPlayback, ...). Required on
         API 34+ alongside the matching <property> on the service below. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Required on API 33+ to post the persistent "pet is active" notification
         that the foreground service is legally required to show. Requested at
         runtime from MainActivity; the service still runs without it, but the
         notification may be silently suppressed by the system. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Lets BootCompletedReceiver restart the overlay after a reboot when the
         user has enabled "Start automatically" in Settings. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:label="Floating Pet Overlay"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Owns the actual floating window's lifecycle; must survive independently
             of MainActivity (see Section 24 of the product spec). Not exported: only
             this app ever needs to start/stop it, via MainActivity or BootCompletedReceiver. -->
        <service
            android:name=".OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Floating animated pet character rendered above other apps" />
        </service>

        <!-- Not exported: BOOT_COMPLETED is a protected system broadcast, so the
             system can still deliver it here without this receiver being exported
             to other apps. -->
        <receiver android:name=".BootCompletedReceiver" android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
